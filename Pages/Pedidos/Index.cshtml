@page
@model VitrineExpress.Pages.Pedidos.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].DataPedido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].ValorTotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].Loja)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].StatusPedido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].TipoEntrega)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Pedido) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataPedido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValorTotal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Loja.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StatusPedido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoEntrega)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
